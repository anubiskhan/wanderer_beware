<h1>Create a story</h1>

<%= form_for [:admin, @story] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit 'Create Story' %>
<% end %>
